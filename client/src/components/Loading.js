import React from 'react';

const Loading = () => {
  return (
    <section>
      <div className="inner">
        <SVGTitle>Loading...</SVGTitle>
      </div>
    </section>
  );
}

export default Loading