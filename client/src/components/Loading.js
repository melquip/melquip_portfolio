import React from 'react';
import SVGTitle from './SVGTitle';

const Loading = () => {
  return (
    <section>
      <div className="inner">
        <SVGTitle>Loading...</SVGTitle>
      </div>
    </section>
  );
}

export default Loading